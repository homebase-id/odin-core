To use you need to also do a "git clone Utilities" and place it in the same
directory as this solution.

Indexer namespace should probably change to something more generic since it
now contains both the indexer, the KV database and the circle database.
