namespace Odin.Services.Peer
{
    public enum MultipartHostTransferParts
    {
        TransferKeyHeader,
        PayloadTransferInstructionSet,
        Metadata,
        Payload,
        Thumbnail
    }
}