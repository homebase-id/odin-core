(function () {
    window.YFChatFunctions = {
        anchorScrollAtBottom: function (element) {
            //console.log("Scrolling to the bottom " + element.id);
            element.scrollTop = element.scrollHeight;
        },

    };
})();